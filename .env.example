# Exemple de fichier .env pour les variables d'environnement
# Copiez ce fichier en .env et ajustez les valeurs

# Niveau de logs (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Chemin du fichier de logs (relatif ou absolu)
LOG_FILE_PATH=logs/app.log

# Fuseau horaire pour les logs (Europe/Paris)
LOG_TIMEZONE=Europe/Paris

# Configuration SMTP pour les emails (si utilisée)
SMTP_SERVER=smtp.example.com
SMTP_FROM_EMAIL=example@example.com
SMTP_TO_EMAIL=example@example.com

# Autres configurations spécifiques à l'app
# Ajouter ici les variables nécessaires